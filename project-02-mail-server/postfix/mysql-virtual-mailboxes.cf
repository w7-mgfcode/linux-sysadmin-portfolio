# Postfix MySQL - Virtual Mailboxes Query
# Used to verify if a mailbox exists for delivery

user = ${MYSQL_USER}
password = ${MYSQL_PASSWORD}
hosts = ${MYSQL_HOST}
dbname = ${MYSQL_DATABASE}

query = SELECT 1 FROM virtual_users WHERE email='%s' AND enabled=1
